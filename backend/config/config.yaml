server:
  port: 8080
  mode: debug

database:
  type: sqlite          # sqlite 或 mysql
  host: ""              # MySQL 地址（sqlite 时留空）
  port: 3306
  user: root
  password: your_password
  dbname: dramamax      # MySQL 数据库名，或 SQLite 文件路径

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

tiktok:
  client_key: your_tiktok_client_key
  client_secret: your_tiktok_client_secret

jwt:
  secret: your_jwt_secret_key_here
